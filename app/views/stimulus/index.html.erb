
<%# data-controller 表明哪个 controller %>
<%# data-xxxx-targer="aaa" 绑定某个 controller 中的 aaaTarger dom%>
<%# data-action="click->hello#greet" 表示action, 什么事件 在哪个 controller 中 的 某个函数%>

<div data-controller="hello">
  <input data-hello-target="name" type="text">
  <button data-action="click->hello#greet">Greet</button>
</div>

<%# data-clipboard-xxx-class 绑定 xxxClass. 只有classList中 包含这个 class。那么才有用 %>
<div data-controller="clipboard" data-clipboard-supported-class="clipboard--supported">
  PIN: <input data-clipboard-target="source" type="text" value="1234" readonly>
  <button data-action="click->clipboard#copy" class="clipboard-button">Copy to Clipboard</button>
</div>

<%# 即使是相同的controller 但是里面的内容独立 %>
<%# <div data-controller="clipboard">
  PIN: <input data-clipboard-target="source" type="text" value="3737" readonly>
  <button data-action="clipboard#copy">Copy to Clipboard</button>
</div> %>


<div data-controller="slideshow" data-slideshow-index-value="1">
  <button data-action="slideshow#previous" data-slideshow-aaa-param="aaa"> ← </button>
  <button data-action="slideshow#next"> → </button>

  <div data-slideshow-target="slide">🐵</div>
  <div data-slideshow-target="slide">🙈</div>
  <div data-slideshow-target="slide">🙉</div>
  <div data-slideshow-target="slide">🙊</div>
</div>

<style>
.clipboard-button {
  display: none;
}

.clipboard--supported .clipboard-button {
  display: initial;
}
</style>
